import { StepMapping } from '../../../field-mapping/FieldMapping';

export const CeipStepMapping: StepMapping = {
    fieldMappings: [
        { name: 'ceipOptIn', isBoolean: true, defaultValue: true }
    ]
}
