# tkr-aws-machine-webhook Package

tkr-aws-machine-webhook management package provides Webhook for AWSMachine to update the AWSMachine resource's specs based on the annotations set by TKR resolver

## Components
