# This verison was previously a managed component
# Converted to unmanaged to work wiht the new build service
# Consider making it a managed component for the next version
name: csi_provisioner
unmanagedComponents: 
  - version: v2.0.0+vmware.1
    gobuildBranch: vmware-2.0.0
    dependencies:
      gobuild.linux_hosttype: linux-centos72-gc32